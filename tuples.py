ex = ('orange','banana','apple')
# no append,no pop,no remove,no append,no insert because tuples are immutable
# only count,index,length,slicing methods are applied
print(ex[:2]) #--->output = ('orange','banana')
